spring:
  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USER_NAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  batch:
    jdbc:
      initialize-schema: never

logging:
  level:
    com.team6.moduply: info
    org.hibernate.SQL: info