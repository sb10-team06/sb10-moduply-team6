# 운영환경 콘텐츠 탐색 시나리오 성능 테스트 보고서

## 1. 테스트 요약

현재 `moduply.co.kr` 운영환경의 애플리케이션 인스턴스 **1대**를 대상으로 콘텐츠 탐색 시나리오를 검증했다.

안정 부하 테스트에서는 사용자 여정 50 TPS를 유지하면서 각 조회 API에 초당 약 50건, 전체적으로는 초당 약 150건의 요청을 발생시켰다. 이 구간에서 HTTP 실패율은 0%였고 전체 응답시간 p95는 64.77ms였다.

추가 한계 탐색에서는 전체 요청이 순간 약 312 RPS에 이르는 구간까지 오류 없이 처리했지만, 응답시간이 크게 증가하고 필요한 VU가 최대치에 도달해 테스트를 조기 중단했다. 따라서 단일 인스턴스의 검증된 안정 처리 성능은 **이 사용자 흐름 기준 전체 약 150 RPS**이며, 약 312 RPS 구간은 안정 성능이 아니라 포화 징후를 확인한 결과로 해석해야 한다.

## 2. 이 시나리오를 선택한 이유

테스트는 실제 사용자가 서비스를 탐색할 때 반복적으로 발생하는 다음 흐름을 재현한다.

```text
로그인 → 콘텐츠 목록 조회 → 콘텐츠 상세 조회 → 리뷰 목록 조회
```

이 흐름을 선택한 이유는 다음과 같다.

- 콘텐츠 탐색은 서비스의 대표적인 읽기 중심 사용자 여정이다.
- 목록, 상세, 리뷰 조회를 연속 호출해 단일 API가 아닌 실제 요청 조합의 처리 성능을 확인할 수 있다.
- 콘텐츠 목록 캐시, 콘텐츠·리뷰 DB 조회, 사용자 인증, DTO 변환 및 이미지 URL 생성처럼 주요 읽기 경로가 함께 사용된다.
- 로그인한 여러 사용자의 JWT를 순환 사용해 단일 토큰만 반복 호출하는 비현실적인 테스트를 피했다.
- 인기 콘텐츠에 요청이 집중되는 운영 특성을 반영해 캐시 효과와 특정 데이터 집중 상황을 함께 확인했다.

로그인은 테스트 시작 단계에서 계정 10개로 한 번씩만 수행했다. 따라서 본 테스트의 처리량은 로그인 API의 반복 성능이 아니라, 로그인된 사용자의 콘텐츠 탐색 성능을 측정한다.

## 3. 공통 테스트 조건

| 항목 | 조건 |
|---|---|
| 대상 | `https://moduply.co.kr` |
| 운영 애플리케이션 인스턴스 | 1대 |
| 부하 도구 | k6, Docker Compose |
| 실행 모델 | `ramping-arrival-rate` |
| 테스트 사용자 | 운영 부하 테스트용 계정 10개 |
| 사용자 선택 | 테스트 전체 iteration에서 10개 JWT 순환 사용 |
| 콘텐츠 목록 | 20개, 평점 내림차순 |
| 리뷰 목록 | 20개, 생성일 내림차순 |
| 인기 데이터 집중 | 상위 10개 콘텐츠에 80% 확률로 접근 |
| 사용자 대기시간 | API 사이 0.2~0.8초 |
| 성공 기준 | HTTP 실패율 1% 미만, 체크 성공률 99% 초과, dropped iteration 0건 |

`ramping-arrival-rate`를 사용한 이유는 서버 응답이 느려져도 VU 반복 횟수에 처리량이 종속되지 않도록 하고, 목표 요청 도착률을 기준으로 서비스가 부하를 감당하는지 확인하기 위해서다.

한 번의 사용자 여정은 조회 API 3개를 호출한다. 따라서 사용자 여정 50 TPS는 API별 약 50 TPS이며, 서비스 전체로는 약 150 RPS에 해당한다.

## 4. 안정 부하 테스트

### 실행 조건

| 항목 | 값 |
|---|---:|
| 시작 사용자 여정 | 20 TPS |
| 단계 | 20 → 35 → 50 → 50 TPS |
| 단계별 시간 | 2분 |
| 종료 감속 | 30초 |
| 사전 할당 VU | 80 |
| 최대 VU | 150 |

50 TPS 구간을 한 단계 더 유지해 단순히 목표치에 도달하는 것뿐 아니라 해당 부하를 지속해서 처리하는지도 확인했다.

### 전체 결과

| 지표 | 결과 |
|---|---:|
| 완료한 사용자 여정 | 17,537회 |
| 전체 요청 | 52,621건 |
| 테스트 전체 평균 요청률 | 102.92 RPS |
| 목표 유지 구간 요청률 | 약 150 RPS |
| HTTP 실패율 | **0.00%** |
| 체크 성공률 | **100.00%** |
| 평균 응답시간 | 35.38ms |
| p90 | 54.95ms |
| p95 | **64.77ms** |
| p99 | 207.21ms |
| 최대 응답시간 | 1,172.98ms |

전체 평균 RPS가 150보다 낮은 이유는 20 TPS부터 단계적으로 증가한 구간과 종료 감속 구간이 평균에 포함되기 때문이다.

### API별 결과

| API | 요청 수 | 실패율 | 평균 | p95 | p99 | 최대 |
|---|---:|---:|---:|---:|---:|---:|
| 콘텐츠 목록 `GET /api/contents` | 17,537 | 0.00% | 62.98ms | **100.11ms** | 408.52ms | 1,172.98ms |
| 콘텐츠 상세 `GET /api/contents/{contentId}` | 17,537 | 0.00% | 22.05ms | **34.83ms** | 115.41ms | 534.66ms |
| 리뷰 목록 `GET /api/reviews` | 17,537 | 0.00% | 21.06ms | **33.81ms** | 113.17ms | 554.13ms |
| 준비 단계 로그인 `POST /api/auth/sign-in` | 10 | 0.00% | 116.34ms | **159.96ms** | 176.35ms | 180.45ms |

단일 운영 인스턴스는 실제 콘텐츠 탐색 흐름에서 전체 약 150 RPS를 처리하면서 오류 없이 낮은 p95 응답시간을 유지했다. 세 조회 API 중 콘텐츠 목록이 가장 무거웠지만 p95 100.11ms로 양호했고, 콘텐츠 상세와 리뷰 목록은 p95 35ms 이내였다.

## 5. 고부하 한계 탐색

### 계획한 조건

| 항목 | 값 |
|---|---:|
| 시작 사용자 여정 | 50 TPS |
| 계획 단계 | 50 → 100 → 135 → 167 → 167 TPS |
| 단계별 시간 | 2분 |
| 사전 할당 VU | 250 |
| 최대 VU | 400 |
| 계획한 최대 전체 요청률 | 약 500 RPS |

이 테스트는 원래 인스턴스가 여러 대라는 가정에서 전체 500 RPS를 검증하도록 설계됐다. 실제 운영환경은 인스턴스 1대였으므로 그대로 진행하면 단일 인스턴스에 전체 부하가 집중된다. 실행 중 응답 지연과 VU 포화 징후를 확인해 약 4분 15초 시점에 안전하게 중단했다.

### 중단 시점 결과

| 지표 | 결과 |
|---|---:|
| 완료한 사용자 여정 | 16,955회 |
| 전체 요청 | 51,258건 |
| 실행 전체 평균 요청률 | 185.74 RPS |
| 중단 직전 사용자 여정 | 약 104 TPS |
| 중단 직전 전체 요청률 | 약 312 RPS |
| HTTP 실패율 | **0.00%** |
| 체크 성공률 | **100.00%** |
| 평균 응답시간 | 447.42ms |
| p90 | 1,433.18ms |
| p95 | **1,950.61ms** |
| p99 | 2,383.69ms |
| 최대 응답시간 | 3,606.26ms |

### API별 결과

| API | 요청 수 | 실패율 | 평균 | p95 | p99 | 판정 |
|---|---:|---:|---:|---:|---:|---|
| 콘텐츠 목록 `GET /api/contents` | 17,218 | 0.00% | 721.53ms | **2,268.24ms** | 2,640.72ms | 지연 한계 초과 |
| 콘텐츠 상세 `GET /api/contents/{contentId}` | 17,075 | 0.00% | 335.43ms | **1,198.89ms** | 1,512.10ms | 지연 한계 초과 |
| 리뷰 목록 `GET /api/reviews` | 16,955 | 0.00% | 282.03ms | **995.23ms** | 1,289.28ms | 목표 경계 |
| 준비 단계 로그인 `POST /api/auth/sign-in` | 10 | 0.00% | 100.88ms | **107.48ms** | 110.51ms | 정상 |

HTTP 오류는 발생하지 않았지만, 오류율 0%만으로 안정적인 성능이라고 판단할 수는 없다. 중단 직전 약 312 RPS에서는 콘텐츠 목록 p95가 2.27초까지 증가했고 최대 VU 400개가 모두 사용됐다. 이는 서버가 요청을 거부하지는 않았지만 처리 지연이 누적되는 포화 상태에 가까웠음을 의미한다.

## 6. 결론

현재 단일 운영 인스턴스에 대해 확인된 성능은 다음과 같이 정리할 수 있다.

- 실제 콘텐츠 탐색 사용자 흐름에서 **사용자 여정 50 TPS**를 지속 처리했다.
- 한 여정이 조회 API 3개를 호출하므로 **전체 약 150 RPS**를 안정적으로 처리한 결과다.
- 안정 구간에서 HTTP 실패율 0%, 체크 성공률 100%, 전체 p95 64.77ms를 기록했다.
- API별 50 TPS에서 콘텐츠 목록 p95 100.11ms, 상세 p95 34.83ms, 리뷰 p95 33.81ms였다.
- 약 312 RPS까지 요청 오류는 없었지만 p95가 1.95초로 증가했으므로 이 수치는 안정 처리량이 아니라 포화 탐색 결과다.

따라서 외부에 설명할 수 있는 보수적인 성능 표현은 다음과 같다.

> 모두의 플리 운영환경은 단일 애플리케이션 인스턴스에서 로그인 사용자의 콘텐츠 목록·상세·리뷰 조회가 이어지는 실제 탐색 시나리오를 기준으로 전체 약 150 RPS를 HTTP 오류 없이 처리했으며, 전체 응답시간 p95 64.77ms를 기록했다.

이 결과는 해당 조회 시나리오와 테스트 데이터 분포에 한정된다. 서비스 전체 API, 쓰기 요청, 장시간 운영 안정성, 순간 트래픽 및 장애 상황의 성능을 대표하지는 않는다.

## 7. 후속 검증 권장사항

- 150~300 RPS 사이를 더 작은 단계로 나눠 단일 인스턴스의 정확한 포화 시작점을 측정한다.
- 고부하 구간에서 애플리케이션 CPU·메모리·GC·스레드·DB 커넥션 풀·Redis·RDB 지표를 함께 수집한다.
- Cold Cache와 Warm Cache를 분리해 콘텐츠 목록 성능 차이를 확인한다.
- dropped iterations와 VU 사용량을 포함한 통과 기준을 보고서의 최종 판정에 반영한다.
- 인스턴스를 확장한 뒤 동일 부하를 재실행해 처리량 증가와 공용 DB·Redis 병목 여부를 비교한다.
